import { RouteLoadingState } from "@/components/orsg/route-state"

export default function Loading() {
  return <RouteLoadingState title="Loading provision" message="Fetching provision context." />
}
