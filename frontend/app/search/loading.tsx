import { RouteLoadingState } from "@/components/orsg/route-state"

export default function Loading() {
  return <RouteLoadingState title="Loading search" message="Preparing results and filters." />
}
