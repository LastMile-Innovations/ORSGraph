import { RouteLoadingState } from "@/components/orsg/route-state"

export default function Loading() {
  return <RouteLoadingState title="Loading ORSGraph" message="Preparing the legal workspace." />
}
